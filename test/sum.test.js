import { sum } from '../src/sum'

test("sum(1, 2) must be 3 one", () => {
  const result = sum(1, 2)
  expect(result).toBe(3)
})
test("sum(1, 2) must be 3 two", () => {
  const result = sum(1, 2)
  expect(result).toBe(3)
})
test("sum(1, 2) must be 3 three", () => {
  const result = sum(1, 2)
  expect(result).toBe(3)
})
